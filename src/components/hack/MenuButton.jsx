import { Button } from '@primer/react';

export default function MenuButton(props) {
  // const { Anchor, setAnchor } = useContext(MenuContext);
  // if (!Anchor) {
  //   setAnchor((anchorProps) => <Button {...anchorProps}></Button>);
  // }
  // return null;
  return <Button {...props} />;
}
